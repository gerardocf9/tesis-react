import React from "react";
import ReactDOM from "react-dom";
import './index.css';

const App = () => (
    console.log(BigInt(4))
    let b = BigInt(3).toString(16)
<div>
  <h1>My React and TypeScript App!</h1>
</div>
);

ReactDOM.render(
  <App />,
  document.getElementById("root")
);
